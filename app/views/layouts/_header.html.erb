<!--  navbar -->
<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    <li><%= link_to "Site Home", store_url, class: 'navbar-brand' %></li>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if !user_signed_in? %>
          <li><%= link_to "sign in", new_user_session_path %></li>
        <% else %>
          <li><%= link_to "Signed in as " + current_user.email, user_path(current_user) %>!</li>
          <% if current_user && current_user.current_cart %>
            <li><%= link_to "Site", site_path(current_user.current_site) %></li>
          <% end %>
          <li><%= link_to("Sign out", destroy_user_session_path, :method => "delete") %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>