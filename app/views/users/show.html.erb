<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
 
<h1><%= @user.name %></h1>
<%= link_to "New Order", new_user_order_path(@user)%>
<p>Tasks:</p>
<% @user.tasks.each do |task| %>
  <div><%= task.name %> - <%= last_updated task %></div>
<% end %>